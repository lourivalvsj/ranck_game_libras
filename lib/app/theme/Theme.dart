import 'dart:ui' show Color;

class SobratColors {
  static const Color black = Color(0xFF000000);

  static const Color white = Color(0xFFFFFFFF);

  static const Color initial = Color.fromRGBO(23, 43, 77, 1.0);

  static const Color primaryLight = Color.fromRGBO(54, 183, 41, 1.0);

  static const Color primaryDark = Color.fromRGBO(54, 183, 41, 1.0);

  static const Color secondary = Color.fromRGBO(247, 250, 252, 1.0);

  static const Color label = Color.fromRGBO(254, 36, 114, 1.0);

  static const Color info = Color.fromRGBO(17, 205, 239, 1.0);

  static const Color error = Color.fromRGBO(245, 54, 92, 1.0);

  static const Color success = Color.fromRGBO(45, 206, 137, 1.0);

  static const Color warning = Color.fromRGBO(251, 99, 64, 1.0);

  static const Color header = Color.fromRGBO(82, 95, 127, 1.0);

  static const Color bgColorScreenLight = Color.fromRGBO(248, 249, 254, 1.0);

  static const Color bgColorScreenDark = Color.fromARGB(255, 37, 37, 37);

  static const Color borderInputEnable = Color.fromRGBO(202, 209, 215, 1.0);

  static const Color borderInputDisable = Color.fromRGBO(232, 239, 246, 1);

  static const Color border = Color.fromRGBO(202, 209, 215, 1.0);

  static const Color inputSuccess = Color.fromRGBO(123, 222, 177, 1.0);

  static const Color inputError = Color.fromRGBO(252, 179, 164, 1.0);

  static const Color muted = Color.fromRGBO(136, 152, 170, 1.0);

  static const Color textLight = Color.fromRGBO(50, 50, 93, 1.0);

  static const Color textDark = Color.fromARGB(255, 209, 209, 218);

  static const Color colorTextButtonLight = Color.fromARGB(255, 255, 255, 255);

  static const Color colorTextButtonDark = Color.fromARGB(255, 255, 255, 255);

  static const Color menuSelectedLight = Color.fromRGBO(229, 229, 229, 1.0);

  static const Color menuSelectedDark = Color.fromRGBO(229, 229, 229, 1.0);

  static const Color btnCancelar = Color.fromRGBO(245, 2, 2, 1);

  static const Color btnVoltar = Color.fromRGBO(53, 107, 195, 1.0);

  static const Color headerTable = Color.fromRGBO(229, 229, 229, 1.0);
}
